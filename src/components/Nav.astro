---
import { site } from '../config/site';
---
<header class="site-header">
  <div class="container">
    <a class="wordmark" href="/" aria-label={site.wordmark}>{site.wordmark}</a>
    <nav class="site-nav" aria-label="Primary">
      <ul>
        {site.nav.map((item) => (
          <li>
            <a
              href={item.href}
              {...item.external ? { target: '_blank', rel: 'noopener' } : {}}
            >
              {item.label}
            </a>
          </li>
        ))}
      </ul>
    </nav>
  </div>
</header>
